version: "3.7"

services:
  command-help:
    build:
      context: .
      dockerfile: ./Dockerfile
    command:
      - "-h"
  runner-client-only:
    build:
      context: .
      dockerfile: ./Dockerfile
    command:
      - "-dot-env-file=/secrets/application.env"
      - "--client"
    volumes:
      - ./application.env:/secrets/application.env:ro
